#include "main.h"

/**
 * _isdigit - check if it is digit.
 *
 * Return: 1 if it is a number.
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
